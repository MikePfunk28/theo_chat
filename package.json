{
  "name": "theo_chat",
  "version": "1.0.0",
  "private": true,
  "description": "TheoChat — YouTube chat bridge for Twitch",
  "scripts": {
    "build": "echo \"Worker is a single file — no build step needed.\"",
    "deploy": "wrangler deploy",
    "deploy:preview": "wrangler deploy --env preview"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "packageManager": "pnpm@9.0.0"
}
