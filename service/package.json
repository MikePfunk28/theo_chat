{
  "name": "theochat-service",
  "version": "1.0.0",
  "description": "YouTube live chat bridge for Twitch — built for theo.gg",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "node --test test/",
    "syntax-check": "node -c index.js && node -c event-log.js && node -c metrics-history.js"
  },
  "dependencies": {
    "@grpc/grpc-js": "^1.14.0",
    "google-protobuf": "^4.0.0",
    "googleapis": "^164.1.0",
    "ws": "^8.18.3"
  },
  "engines": {
    "node": ">=18"
  }
}
